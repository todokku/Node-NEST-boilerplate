import { NotFoundException } from "@nestjs/common";
export declare const constants: {
    admin: {
        name: string;
        email: string;
        password: string;
    };
    notFound: NotFoundException;
};
